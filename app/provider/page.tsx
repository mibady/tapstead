import { ProviderPageClient } from "./ProviderPageClient"

export const metadata = {
  title: "Provider Dashboard | Tapstead Pro",
  description: "Manage your bookings, schedule, and earnings as a Tapstead professional service provider.",
}

export default function ProviderPage() {
  return <ProviderPageClient />
}
